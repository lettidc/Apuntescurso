//Como acceder a un array

const numeros = [10,20,30,40,50];

// muy util para Arrays
console.table(numeros);

//la forma de acceder a un array se conoce como indice del array
//los arrays en JS inician en la posicion 0
console.log(numeros[0]);

console.log(numeros[2]);
//como no hay elemento lo muestra como Undefined
console.log(numeros[20]);