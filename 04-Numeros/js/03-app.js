//Math es parte de la ventana principal y goblal de JS
//tiene una serie de operaciones que pueden ser muy útiles
//y algunas actuan de modo extraño, así que vamos allá

let resultado;

resultado=Math.PI;//numero pi

resultado=Math.round(2.6);//Redondear una cifra

resultado=Math.ceil(2.00000000000001); //redondeo hacia arriba
resultado=Math.floor(2.999991); //redondeo hacia abajo

resultado=Math.sqrt(144);//raiz cuadrada

resultado=Math.abs(-555555);//valor absoluto

resultado=Math.pow(8,4);//potencia

resultado=Math.max(47,32,11,60,-2);//min y max de un rango de valores

resultado=Math.random();// aleatorio entre 0 y 1 con valores reales

resultado=Math.floor(Math.random()*50);//Aleatorio entre un rango ligeramente tratado

resultado=Math.E;//numero E
console.log(resultado);




